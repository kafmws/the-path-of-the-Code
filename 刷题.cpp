#include<stdio.h>
#include<math.h>
#include<string.h>
#include<stdlib.h>
#define max(a,b) a>b?a:b
#define min(a,b) a>b?b:a

int main() {
	int floor,i,n,time,num[55],row;
	char str[10];
	scanf("%d",&n);
	while(n--){
		floor=0;
		scanf("%d",&hang);
		for(i=0;i<row;i++){
			scanf("%s",&str);
			if(str[0]=='I'){
				
			}
		}
	}
	return 0;
}
