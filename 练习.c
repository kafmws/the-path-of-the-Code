#include<stdio.h>
#include<string.h>
int main() {
	int i,j,x,n=0;
	char str[10][10][30],name[10][10],c='0';
	do {
		scanf("%d",&x);
		for(i=0; i<x; i++) {
			for(j=0;j<4; j++)
				while(c!='\n') {scanf("%d",str[n][i][j]);
					if(c!=' '){
						if()
					}
					c=getchar();
				}
		}
		n++;
	} while(x!=-1);

	return 0;
}

